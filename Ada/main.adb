--With Ada.Text_IO; Use Ada.Text_IO;  
--With Ada.Integer_Text_IO; Use Ada.Integer_Text_IO;
 
--procedure Program is
--begin
  -- your code goes here
--end Program;
 
--with Ada.Text_IO;
--use Ada.Text_IO;
 
--procedUre Main is
--begin
	--puT_Line ("Hello world");
--	Ada.Text_Io.Put_Line ("Hello Wrld");
--end Main;
with Ada.Text_IO; use ada.text_io;
 
procedure Main is
	-- variable declarations
	A, B : Integer := 0;
	C    : Integer := 100;
	D    : Integer;
--	Variable : Integer := 0;
begin
	A := A + 1;
	D := A + B + c;
	put_line ("Hello");
	put_line (Integer'Image (D));
 
-- Outputs 0, 1, 2, ..., 9
for Var in 0 .. 9 loop
	Put_Line (Integer'Image (Var));
	put_line (Integer'Image (var));
	--null;
 
 
end loop;
 
end Main;

